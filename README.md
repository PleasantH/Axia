Axia Second Assessment

This is an expressJs project showing CRUD operations by creating, fetching, updating and deleting users.

## Getting Started
First, install the dependencies:
npm install

Then, start the server

npm run dev